{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "a single page budget tracking application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon server",
    "client-install": "cd client && npm install",
    "client-start": "cd client && npm run start",
    "heroku-postbuild": "npm run client-install && npm run client-start"
  },
  "author": "Richard Wang",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^5.9.5"
  }
}
